# Agent Artifacts

All committed agent artifacts must live under `.agent/`.

Examples:

- superpowers plans
- agent planning notes
- agent reports
- reviewed scratch artifacts that are useful to keep with the branch

Do not place agent artifacts under `docs/`, source packages, migrations, or runtime config directories unless they are converted into user-facing product documentation or production code.
