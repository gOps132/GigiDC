# Workflow Knowledge

- 2026-04-01: GigiDC already had a strong project-memory mechanism in `AGENTS.md`, so a separate knowledge system should complement it rather than duplicate it.
- 2026-04-01: For this repo, domain-based knowledge capture fits best as lightweight documentation under `knowledge/` instead of a second architecture or operations doc tree.
- 2026-04-01: `docs/docs.json` is the source of truth for docs navigation, so any new standing workflow doc should be added there when it is relevant to maintainers.
